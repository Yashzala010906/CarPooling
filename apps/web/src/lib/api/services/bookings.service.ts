/**
 * create, cancel, list bookings
 * Base path: /bookings
 */
export const bookingsService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/bookings')).data,
  // getById: async (id: string) => (await apiClient.get(`/bookings/${id}`)).data,
};
