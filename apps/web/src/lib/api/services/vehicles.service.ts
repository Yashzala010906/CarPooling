/**
 * vehicle CRUD
 * Base path: /vehicles
 */
export const vehiclesService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/vehicles')).data,
  // getById: async (id: string) => (await apiClient.get(`/vehicles/${id}`)).data,
};
