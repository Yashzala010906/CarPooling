/**
 * trips, distance, fuel, cost analytics
 * Base path: /reports
 */
export const reportsService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/reports')).data,
  // getById: async (id: string) => (await apiClient.get(`/reports/${id}`)).data,
};
