/**
 * company admin: employees, settings
 * Base path: /company
 */
export const companyService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/company')).data,
  // getById: async (id: string) => (await apiClient.get(`/company/${id}`)).data,
};
