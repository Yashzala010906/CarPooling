/**
 * search, publish, ride details
 * Base path: /rides
 */
export const ridesService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/rides')).data,
  // getById: async (id: string) => (await apiClient.get(`/rides/${id}`)).data,
};
