/**
 * list, mark read
 * Base path: /notifications
 */
export const notificationsService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/notifications')).data,
  // getById: async (id: string) => (await apiClient.get(`/notifications/${id}`)).data,
};
