/**
 * create payment, Razorpay order/verify
 * Base path: /payments
 */
export const paymentsService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/payments')).data,
  // getById: async (id: string) => (await apiClient.get(`/payments/${id}`)).data,
};
