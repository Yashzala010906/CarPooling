/**
 * profile CRUD
 * Base path: /users
 */
export const usersService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/users')).data,
  // getById: async (id: string) => (await apiClient.get(`/users/${id}`)).data,
};
