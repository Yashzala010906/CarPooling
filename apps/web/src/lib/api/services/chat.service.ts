/**
 * trip message history (live messages via socket)
 * Base path: /chat
 */
export const chatService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/chat')).data,
  // getById: async (id: string) => (await apiClient.get(`/chat/${id}`)).data,
};
