/**
 * login, register, refresh, logout, me
 * Base path: /auth
 */
export const authService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/auth')).data,
  // getById: async (id: string) => (await apiClient.get(`/auth/${id}`)).data,
};
