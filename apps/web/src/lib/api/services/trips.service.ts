/**
 * my trips, trip lifecycle, ride history
 * Base path: /trips
 */
export const tripsService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/trips')).data,
  // getById: async (id: string) => (await apiClient.get(`/trips/${id}`)).data,
};
