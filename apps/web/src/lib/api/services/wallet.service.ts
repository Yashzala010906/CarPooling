/**
 * balance, recharge, transactions
 * Base path: /wallet
 */
export const walletService = {
  // TODO: implement endpoints using apiClient from '../client', e.g.
  // import { apiClient } from '../client';
  // list: async () => (await apiClient.get('/wallet')).data,
  // getById: async (id: string) => (await apiClient.get(`/wallet/${id}`)).data,
};
